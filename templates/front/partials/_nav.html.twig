{% set currentRoute = app.request.get('_route') %}

<nav class="navbar navbar-expand-lg">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('home') }}">
			<img src="{{ asset('assets/img/logo.png') }}" alt="logo JBMsy" class="nav-logo d-inline-block align-text-top">
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon icon-burger-menu  d-fex align-items-center">
				<i class="fa fa-dot-circle-o" aria-hidden="true"></i>
			</span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link mx-2 {{ currentRoute == 'project_index' ? 'nav-link-active':'' }}" href="{{ path('project_index') }}">Projets</a>
				</li>
				<li class="nav-item">
					<a class="nav-link mx-2" href="mailto:jbmoisy.dev@gmail.com">Contact</a>
				</li>
				{% if app.user %}
					{% if is_granted('ROLE_ADMIN') %}
						<li class="nav-item">
							<a class="nav-link mx-2" href="{{ path('back') }}">BackOffice</a>
						</li>
						<li class="nav-item">
							<a class="nav-link mx-2" href="{{ path('app_logout') }}">Déconnexion</a>
						</li>
					{% endif %}
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
