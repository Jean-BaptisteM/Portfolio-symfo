{% extends 'back/base.html.twig' %}

{% block title %}
	Versionning :
	{{ versionning.name }}
{% endblock %}

{% block body %}
	<h1 class="back-title">Versionning :
		{{ versionning.name }}</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ versionning.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ versionning.name }}</td>
			</tr>
		</tbody>
	</table>
	<div class="d-flex flex-column">
		<a href="{{ path('app_back_versionning_index') }}" class="btn my-2 col-4">Retour à la liste</a>

		<a href="{{ path('app_back_versionning_edit', {'id': versionning.id}) }}" class="btn my-2 col-4">Modifier</a>

		{{ include('back/versionning/_delete_form.html.twig') }}
	</div>
{% endblock %}
