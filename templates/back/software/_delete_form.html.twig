<form method="post" action="{{ path('app_back_software_delete', {'id': software.id}) }}" onsubmit="return confirm('Etes-vous certain de vouloir supprimer ce Software ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ software.id) }}">
    <button class="btn my-2 col-4">Supprimer</button>
</form>
