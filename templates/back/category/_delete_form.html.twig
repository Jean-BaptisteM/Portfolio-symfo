<form method="post" action="{{ path('app_back_category_delete', {'id': category.id}) }}" onsubmit="return confirm('Etes-vous certain de vouloir supprimer cette catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="btn my-2 col-4">Supprimer</button>
</form>
