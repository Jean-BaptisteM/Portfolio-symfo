<form method="post" action="{{ path('app_back_database_delete', {'id': database.id}) }}" onsubmit="return confirm('Etes-vous certain de vouloir supprimer ce langage de Database ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ database.id) }}">
    <button class="btn my-2 col-4">Supprimer</button>
</form>
