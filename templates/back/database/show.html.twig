{% extends 'back/base.html.twig' %}

{% block title %}
	Langage de Database :
	{{ database.name }}
{% endblock %}

{% block body %}
	<h1 class="back-title">Langage de Database :
		{{ database.name }}</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ database.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ database.name }}</td>
			</tr>
		</tbody>
	</table>
	<div class="d-flex flex-column">
		<a href="{{ path('app_back_database_index') }}" class="btn my-2 col-4">Retour à la liste</a>

		<a href="{{ path('app_back_database_edit', {'id': database.id}) }}" class="btn my-2 col-4">Modifier</a>

		{{ include('back/database/_delete_form.html.twig') }}
	</div>
{% endblock %}
