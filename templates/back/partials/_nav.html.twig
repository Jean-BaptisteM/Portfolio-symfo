{% set currentRoute = app.request.get('_route') %}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('back')}}">
			<img src="{{ asset('assets/img/backoffice-logo.png') }}" alt="" class="nav-logo-back d-inline-block align-text-top">
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_back_user_show', {id:app.user.id}) }}">Mon Profil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link {{ currentRoute == 'app_back_project_index' ? 'nav-link-active':'' }}" href="{{ path('app_back_project_index')}}">Projets</a>
				</li>
				<li class="nav-item">
					<a class="nav-link {{ currentRoute == 'app_back_dashboard_index' ? 'nav-link-active':'' }}" href="{{ path('app_back_dashboard_index')}}">Dashboard</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('home')}}">FrontOffice</a>
				</li>
				{% if app.user %}
						<li class="nav-item">
							<a class="nav-link mx-2" href="{{ path('app_logout') }}">Déconnexion</a>
						</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
